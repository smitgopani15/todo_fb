class DataModal {
  String? id, title, description;

  DataModal({this.id, this.title, this.description});
}

class UserModal {
  String? username, email;

  UserModal({this.username, this.email});
}
